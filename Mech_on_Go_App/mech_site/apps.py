# from django.apps import AppConfig


# class MechSiteConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'mech_site'

from django.apps import AppConfig

class MechSiteConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'mech_site'  # Ensure this matches your folder name

